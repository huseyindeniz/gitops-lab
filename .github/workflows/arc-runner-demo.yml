name: arc-runner test
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: arc-runner-local-staging
    steps:
      - name: greeting
        run: echo "🎉 This job uses arc-runner on local-staging cluster!"
