name: MySampleApp1 - IntegrationTests

on:
  pull_request:
    paths:
      - 'apps/my-sample-app-1/dotnet-services/**'
  workflow_dispatch:

jobs:        
  service-int-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - weatherForecast
        include:
          - service: weatherForecast
            project_path: apps/my-sample-app-1/dotnet-services/mySampleApp1.wf.tests.integration

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
          
      - name: Setup .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'
          
      - name: Restore dependencies
        run: dotnet restore apps/my-sample-app-1/dotnet-services/my-sample-app-1.sln

      - name: Build the project
        run: dotnet build apps/my-sample-app-1/dotnet-services/my-sample-app-1.sln --configuration Release --no-restore
    
      - name: Run Integration Tests for ${{ matrix.service }}
        run: |
          dotnet test --no-restore --configuration Release --filter "Category=Integration&Service=weatherForecast" --logger "trx;LogFileName=mySampleApp1-wf-tests.integration.trx"
        working-directory: ${{ matrix.project_path }}
        
      - name: Upload Results for ${{ matrix.service }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.service }}-reports
          path: ${{ matrix.project_path }}/TestResults/*.trx
