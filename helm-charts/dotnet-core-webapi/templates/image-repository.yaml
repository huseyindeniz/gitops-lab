apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageRepository
metadata:
  name: {{ .Release.Name }}-app-image-repository
  namespace: {{ include "dotnet-core-webapi.namespace" . }}
spec:
  interval: {{ .Values.imageRepository.interval }}
  image: "{{ .Values.image.app.repository }}"

---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageRepository
metadata:
  name: {{ .Release.Name }}-migration-image-repository
  namespace: {{ include "dotnet-core-webapi.namespace" . }}
spec:
  interval: {{ .Values.imageRepository.interval }}
  image: "{{ .Values.image.migration.repository }}"
