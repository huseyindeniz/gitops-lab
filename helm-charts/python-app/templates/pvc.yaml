apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sample-ai-backend-pvc
  labels: ""
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi # Must match PV capacity
  volumeName: wsl-shared-volume-pv # Explicitly bind to the existing PV
