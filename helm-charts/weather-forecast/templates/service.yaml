apiVersion: v1
kind: Service
metadata:
  labels:
    name: weather-forecast-webapi
  name: weather-forecast-webapi
spec:
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
  selector:
    app: weather-forecast-webapi
