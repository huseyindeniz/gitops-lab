apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: harbor-service-entry
  namespace: istio-system
spec:
  hosts:
    - "harbor.staging.local"
  location: MESH_INTERNAL
  resolution: DNS
  addresses:
    - 10.110.23.162 # cluster ip of istio-ingressgateway-staging
  ports:
    - name: http
      number: 31879 # istio ingress gateway http NodePort
      protocol: HTTP
    - name: https
      number: 32395 # istio ingress gateway https NodePort
      protocol: HTTPS
