apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gitopslab
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/huseyindeniz/gitops-lab.git
  branch: main
  secretRef:
    name: flux-github-secret
