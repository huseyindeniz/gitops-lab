password: "12345"

storage:
  size: 1Gi

service:
  ingress:
    enabled: true
    hosts:
      - jupyterlab.wsl.local

resources:
  requests:
    nvidia.com/gpu: "1"
  limits:
    nvidia.com/gpu: "1"
