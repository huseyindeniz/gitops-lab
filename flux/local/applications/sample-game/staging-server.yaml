apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: sample-game-server-local-appset-staging
  namespace: argo-local
spec:
  generators:
    - list:
        elements:
          - envName: staging
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  template:
    metadata:
      name: "sample-game-server-{{.envName}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/huseyindeniz/gitops-lab.git
        targetRevision: main
        path: "helm-charts/nodejs-app"
        helm:
          valueFiles:
            - values.yaml
            - $valuesRef/flux/local/applications/sample-game/values/staging-server-values.yaml
      destination:
        server: "https://kubernetes.default.svc"
        namespace: "sample-game-server-local-{{ .envName }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        retry:
          limit: 1
        syncOptions:
          - SkipHooksOnError=true # Ensures Argo CD skips retries if a Helm hook fails
