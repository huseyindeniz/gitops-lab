loki:
  auth_enabled: false
storage_config:
  s3:
    bucketnames: default
    endpoint: minio-staging.minio.svc.cluster.local:9000
    access_key_id: minio
    secret_access_key: minio123
    insecure: true
