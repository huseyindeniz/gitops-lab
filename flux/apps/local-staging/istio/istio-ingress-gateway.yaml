apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-ingress
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: "https://istio-release.storage.googleapis.com/charts"
      chart: "gateway"
      targetRevision: "1.25.0-alpha.0"
      helm:
        valueFiles:
          - "$values/flux/apps/local-staging/istio/values/istio-ingress-gateway.yaml"
    - repoURL: "https://github.com/huseyindeniz/gitops-lab.git"
      targetRevision: main
      ref: values
  destination:
    server: "https://172.17.0.5:8443"
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
