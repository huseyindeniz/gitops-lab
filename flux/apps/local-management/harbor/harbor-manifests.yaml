apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor-manifests
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/huseyindeniz/gitops-lab.git"
    path: "flux/manifests/local-management/harbor"
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
