# Disable optional subcharts
metrics-server:
  enabled: false

cert-manager:
  enabled: false

nginx:
  enabled: false

kong:
  enabled: true
  proxy:
    http:
      enabled: true

# Fixed csrfKey to prevent OutOfSync on every ArgoCD sync
app:
  security:
    csrfKey: "MTIz"
