apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb-manifests
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/huseyindeniz/flux/local-management/manifests"
    path: "metallb"
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
