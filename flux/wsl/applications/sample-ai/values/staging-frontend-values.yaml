replicaCount: 1

image:
  repository: sample-ai-frontend # not published to docker hub, exist in my local docker
  pullPolicy: IfNotPresent
  tag: "latest"

nameOverride: "sample-ai-frontend"
fullnameOverride: "sample-ai-frontend-staging"

podAnnotations: {}
podLabels: {}

service:
  type: LoadBalancer
  port: 8080
  targetPort: 80

ingress:
  enabled: true
  className: ""
  annotations:
    {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: sample-ai-frontend-staging.local
      paths:
        - path: /
          pathType: Prefix

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80
