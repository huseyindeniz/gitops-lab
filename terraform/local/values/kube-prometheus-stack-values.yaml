prometheus:
  persistance:
    enabled: true
    existingClaim: prometheus_pvc
  retention: 7d
  resources:
    requests:
      memory: 500Mi
      cpu: 500m
    limits:
      memory: 1Gi
      cpu: 1 
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - prometheus.local
grafana:
  adminPassword: 12345aA.
  persistence:
    enabled: true
    existingClaim: grafana-pvc
  resources:
    requests:
      memory: 500Mi
      cpu: 250m
    limits:
      memory: 1Gi
      cpu: 500m    
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.local