prometheus:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - prometheus.wsl
grafana:
  adminPassword: 12345aA.
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.wsl
