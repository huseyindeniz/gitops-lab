global:
  image:
    tag: "v2.12.1"

server:
  extraArgs:
    - --insecure

crds:
  # -- this fixes CustomResourceDefinition "applications.argoproj.io" in namespace "" exists error
  install: false
