architecture: standalone

replica:
  replicaCount: 1

auth:
  enabled: false
  password: "12345"
  usePasswordFiles: false

volumePermissions:
  enabled: true

master:
  persistence:
    enabled: true
    size: 1Gi
    storageClass: "standard"

metrics:
  enabled: true
