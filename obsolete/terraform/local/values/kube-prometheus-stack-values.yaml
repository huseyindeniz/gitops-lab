prometheus:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - prometheus.local
grafana:
  adminPassword: 12345aA.
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.local
