apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: k8s-dashboard-vs
  namespace: istio-system
spec:
  hosts:
    - "dashboard.prod.local"
  gateways:
    - istio-ingressgateway-production
  http:
    - match:
        - uri:
            prefix: /api/
      route:
        - destination:
            host: k8sdashboard-prod-kubernetes-dashboard-api.kubernetes-dashboard.svc.cluster.local
            port:
              number: 8000

    - match:
        - uri:
            prefix: /auth/
      route:
        - destination:
            host: k8sdashboard-prod-kubernetes-dashboard-auth.kubernetes-dashboard.svc.cluster.local
            port:
              number: 8000

    - match:
        - uri:
            prefix: /metrics/
      route:
        - destination:
            host: k8sdashboard-prod-kubernetes-dashboard-metrics-scraper.kubernetes-dashboard.svc.cluster.local
            port:
              number: 8000

    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: k8sdashboard-prod-kubernetes-dashboard-web.kubernetes-dashboard.svc.cluster.local
            port:
              number: 8000
